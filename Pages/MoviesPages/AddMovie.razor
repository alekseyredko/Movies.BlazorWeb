@page "/movies/add"

<h3>AddMovie</h3>

<form>
    <ErrorResult Result="addResult"/>
    <p>Movie name:</p>
    <SpecificErrorResult Result="addResult" Key="MovieName" />
    <input type="text" @bind="@movieRequest.MovieName" />

    <p>Duration:</p>
    <input type="text" @bind="@movieRequest.Duration" />

    <button @onclick="AddMovieAsync">Submit</button>
</form>